{
  "build": {
    "command": "composer install --no-dev --optimize-autoloader && npm install && npm run build && php artisan key:generate --force && php artisan migrate --force && php artisan config:cache"
  },
  "start": {
    "command": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
